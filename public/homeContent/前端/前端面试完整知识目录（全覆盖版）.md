# 前端面试完整知识目录（全覆盖版）

说明：本目录涵盖前端面试所有核心知识点，按模块分类，可直接作为复习、刷题的核心框架，重点模块已标注，适配各类前端岗位面试需求。

# 一、HTML 核心

- HTML 基础语法、标签语义化

- 块级元素/行内元素/行内块元素的区别及应用

- 常用语义标签（header、footer、section、article、nav 等）的使用场景

- 表单标签（input、select、textarea 等）及原生校验规则

- iframe、a 标签、img 标签的特性及注意事项

- HTML5 新特性
        

    - 音视频（audio、video）、canvas、svg 用法

    - 本地存储（localStorage、sessionStorage）、离线缓存（Application Cache）

    - 地理定位（Geolocation）、拖拽 API、Web Worker

- meta 标签配置、viewport 适配、SEO 基础优化

# 二、CSS 核心

## 2\.1 基础知识点

- 选择器：基础选择器、层级选择器、伪类选择器、伪元素选择器、属性选择器

- 选择器优先级、权重计算规则、\!important 的作用及注意事项

- CSS 继承性、层叠上下文的原理及应用

## 2\.2 布局相关

- 标准流、浮动（float）及清除浮动的方法

- 定位：relative、absolute、fixed、sticky 的区别及应用场景

- Flex 布局：容器属性、项目属性、居中实现、自适应布局

- Grid 布局：行列定义、网格区域、适配布局、与 Flex 布局对比

- 经典布局：圣杯布局、双飞翼布局的实现及原理

- 弹性居中的多种实现方案（Flex、Grid、定位\+transform 等）

## 2\.3 样式特性

- BFC 原理、触发条件及实际应用场景

- 盒模型：标准盒模型与怪异盒模型的区别、box\-sizing 属性的作用

- 边距合并、外边距塌陷的原因及解决方法

- 元素隐藏方式对比（display: none、visibility: hidden、opacity: 0 等）

## 2\.4 CSS3 进阶

- 圆角（border\-radius）、阴影（box\-shadow、text\-shadow）、渐变（linear\-gradient、radial\-gradient）

- 2D/3D 变换（transform）：平移、旋转、缩放、倾斜

- 过渡（transition）、动画（animation）的用法及属性配置

- 媒体查询（media query）与响应式布局实现

- 单位区别：rem、em、vw、vh、px，移动端适配方案

- CSS 预处理器：Sass/Less 基础语法、变量、混合、嵌套

- CSS 模块化、Scoped 原理、样式隔离方案

- 原子化 CSS（Tailwind CSS）的特点及使用场景

# 三、JavaScript 基础（重中之重）

## 3\.1 语法基础

- 变量声明：var、let、const 的区别（作用域、提升、重复声明）

- 数据类型：基本数据类型（7种）、引用数据类型，类型判断方法

- 类型转换：显式转换、隐式转换的规则及常见场景

- 运算符：算术运算符、比较运算符、逻辑运算符、三元运算符，逻辑短路特性

## 3\.2 核心概念

- 作用域：全局作用域、函数作用域、块级作用域

- 作用域链、闭包：原理、应用场景、潜在问题（内存泄漏）

- 执行上下文、调用栈的工作机制

- this 指向：普通函数、箭头函数、构造函数、事件绑定中的 this 指向及改变方法

- 原型、原型链：构造函数、原型对象、实例的关系，继承的实现方式

- 构造函数、实例、静态属性/方法的区别

## 3\.3 数组 \&amp; 字符串

- 数组常用方法：遍历（forEach、for\.\.\.of）、增删改查（push、pop、shift、unshift、splice、slice）

- 数组高阶函数：map、filter、reduce、some、every 的用法及实现原理

- 数组常见操作：去重、扁平化、排序（冒泡、选择、快速排序）

- 字符串常用方法、正则表达式（匹配、替换、验证）

## 3\.4 异步 JS

- 同步与异步的区别，宏任务与微任务的分类

- 事件循环（EventLoop）的完整流程（浏览器端、Node 端）

- 回调地狱的问题及解决方案

- Promise：原理、状态、常用方法（then、catch、finally）、链式调用

- async/await 语法、异步串行与并行实现

- Generator 函数的基本用法及了解

## 3\.5 ES6\+ 全知识点

- 解构赋值（数组解构、对象解构）、模板字符串

- 箭头函数、剩余运算符（\.\.\.）、扩展运算符（\.\.\.）

- 数据结构：Set、Map、WeakSet、WeakMap 的区别及应用

- Symbol：特性、作用（避免属性名冲突）

- 迭代器（Iterator）、可迭代对象

- 类（class）、模块化（import/export）

- 可选链（?\.）、空值合并（??）、逻辑赋值运算符

## 3\.6 其他重点

- 垃圾回收机制（标记清除、引用计数）、内存泄漏的原因及解决方法

- 深拷贝与浅拷贝的区别及实现方法

- 防抖与节流：原理、实现方法、应用场景

- 手写常见 JS 方法：call、apply、bind、new、instanceof

# 四、DOM \&amp; BOM

## 4\.1 DOM

- DOM 节点类型（元素节点、文本节点、属性节点）、节点操作（增删改查）

- 事件绑定：传统绑定、事件监听（addEventListener）、事件解绑

- 事件流：事件冒泡、事件捕获、事件委托的原理及应用

- 常用事件：鼠标事件、键盘事件、滚动事件、加载事件

- 虚拟 DOM 与真实 DOM 的区别、虚拟 DOM 的优势

## 4\.2 BOM

- BOM 核心对象：window、navigator、location、history 的常用方法

- 定时器：setTimeout、setInterval 的用法及注意事项（this 指向、清除定时器）

- 本地存储：localStorage、sessionStorage、cookie 的区别及应用

- cookie 的特性、有效期、作用域、跨域限制

# 五、网络基础（前端必问）

- HTTP 与 HTTPS 的区别（加密、端口、安全性）

- HTTP 版本差异：HTTP1\.0、HTTP1\.1、HTTP2\.0、HTTP3\.0 的核心改进

- 常见请求方法：GET 与 POST 的区别，其他请求方法（PUT、DELETE、PATCH 等）

- HTTP 状态码：2xx（成功）、3xx（重定向）、4xx（客户端错误）、5xx（服务端错误）的常用状态码及含义

- 请求头、响应头的常见字段（Content\-Type、Cookie、Authorization 等）

- 缓存机制：强缓存（Cache\-Control、Expires）、协商缓存（ETag、Last\-Modified）的原理及应用

- 跨域问题
        

    - 跨域产生的原因（同源策略）

    - 解决方式：CORS、JSONP、代理（开发环境）、Nginx 反向代理、WebSocket

- 接口鉴权：Cookie/Session、Token、JWT 的原理及区别

- WebSocket 原理、特点及应用场景

- TCP 三次握手、四次挥手的流程，TCP 与 UDP 的区别

# 六、前端工程化

- Webpack
        

    - 核心概念：入口（entry）、出口（output）、loader、plugin

    - 常用 loader（css\-loader、style\-loader、babel\-loader 等）、常用 plugin（HtmlWebpackPlugin、CleanWebpackPlugin 等）

    - 打包优化：分包、懒加载、tree\-shaking、压缩资源

    - 热更新原理、环境变量配置（开发环境、生产环境）

- Vite 原理、与 Webpack 的对比（构建速度、原理差异）

- 模块化发展：IIFE、CommonJS、AMD、ESM 的区别及应用

- Git 常用命令、分支管理（git branch、git merge、git rebase 等）

- ESLint、Prettier 的作用及配置，代码规范落地

- 包管理工具：npm、yarn、pnpm 的区别及使用

- 项目构建、部署流程（打包、测试、上线）

# 七、框架核心（Vue \+ React）

## 7\.1 Vue 全家桶

- Vue2 与 Vue3 的核心区别（响应式原理、API 风格、性能等）

- 响应式原理：Vue2（Object\.defineProperty）、Vue3（Proxy）的实现及差异

- Vue 生命周期（Vue2、Vue3）、父子组件生命周期执行顺序

- 组件通信：8 种常用方式（Props/emit、Vuex/Pinia、事件总线、provide/inject 等）

- v\-model 原理、双向绑定的实现

- 计算属性（computed）与侦听属性（watch）的区别及应用

- 虚拟 DOM、diff 算法原理、key 的作用

- 插槽（普通插槽、作用域插槽）、混入（Mixin）、组合式 API（setup、ref、reactive 等）

- Vue\-Router
        

    - 路由模式：hash 模式与 history 模式的原理及区别

    - 路由守卫（全局、路由独享、组件内）、动态路由、路由懒加载

- Vuex / Pinia：状态管理的核心概念、使用方法及原理区别

## 7\.2 React 全家桶

- 类组件与函数组件的区别及应用场景

- React 生命周期（类组件）、Hooks 全掌握
        

    - 基础 Hooks：useState、useEffect、useRef

    - 优化 Hooks：useMemo、useCallback、useContext

- 组件通信：Props 传递、Context API、事件总线等

- 虚拟 DOM、React 调和算法（Reconciliation）

- 状态管理：Redux、MobX、Zustand 的使用及原理

- React\-Router：路由配置、路由守卫、嵌套路由、动态路由

- 受控组件与非受控组件的区别及应用

- React 性能优化方案（减少重渲染、懒加载、代码分割等）

# 八、小程序 / UniApp（可选，看岗位需求）

- 小程序双线程架构（渲染层、逻辑层）的原理

- 小程序生命周期（应用生命周期、页面生命周期）、页面跳转方式

- 小程序组件通信、自定义组件的开发与使用

- 小程序登录流程、授权机制（用户信息、权限申请）

- UniApp 跨端原理、与原生小程序的区别

# 九、性能优化（高频面试大题）

- 页面加载优化（减少请求数、优化请求顺序、预加载）

- 资源优化：图片（压缩、懒加载、WebP 格式）、JS/CSS 压缩与合并、字体优化

- 代码层面优化（减少冗余代码、合理使用缓存、避免内存泄漏）

- 渲染优化：重排（回流）、重绘的原因及避免方法，合成层的使用

- 首屏优化、白屏优化的核心方案（骨架屏、预渲染、SSR 等）

- 懒加载、预加载、骨架屏的实现方式

- 打包体积优化（tree\-shaking、分包、按需加载）

- 性能分析工具：Lighthouse、Chrome 开发者工具的使用

# 十、手写面试题（必背）

- 手写防抖、节流函数（带立即执行、取消功能）

- 手写深拷贝、浅拷贝（处理循环引用）

- 手写 call、apply、bind 方法

- 手写 new 操作符、instanceof 方法

- 手写 Promise 源码（实现 then、catch、all、race 等方法）

- 手写数组扁平化、去重、排序函数

- 手写事件总线（EventBus）

- 手写简易 AJAX、fetch 封装（带拦截器、错误处理）

# 十一、浏览器原理

- 浏览器渲染流程（解析 HTML、CSS，构建 DOM、CSSOM，布局、绘制、合成）

- 从输入网址到页面展示的全过程（DNS 解析、TCP 连接、HTTP 请求、渲染等）

- 回流（重排）、重绘、图层合成的原理及关系

- 浏览器安全机制：XSS（跨站脚本攻击）、CSRF（跨站请求伪造）的原理及防御方法

- 同源策略的定义、作用及跨域的根源

# 十二、算法与手撕代码

- 数组、字符串高频算法题（两数之和、最长公共前缀、字符串反转等）

- 常用算法思想：双指针、递归、回溯、动态规划（基础）

- 排序算法：冒泡排序、选择排序、快速排序、归并排序（原理及实现）

- 前端常考 LeetCode 题库（简单\+中等难度，重点关注数组、字符串、链表类题目）

> （注：文档部分内容可能由 AI 生成）
