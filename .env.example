# 动态后端根地址（不配置则与改造前完全一致：仅 localStorage + public JSON）
# 联调：Next 默认 3000、CRA 默认 3000 会冲突，可任选其一改端口，例如：
#   set PORT=3001; npm start   （PowerShell：CRA 用 3001）
#   REACT_APP_API_BASE_URL=http://localhost:3000
