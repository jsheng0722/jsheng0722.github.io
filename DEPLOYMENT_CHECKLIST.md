# GitHub Pages 部署检查清单

## ✅ 已修复的问题

### 1. 首页路径问题
- ✅ `src/components/Layout/Header/Header.js` - 导航链接从 `/home` 改为 `/`
- ✅ `src/components/Desktop/Desktop.js` - 快捷方式从 `/home` 改为 `/`
- ✅ 路由配置：`/` 指向 HomePage，`/home` 仍然可用（向后兼容）

## 🔍 内容不一致问题排查

### 可能的原因

1. **环境变量差异**
   - 开发环境：`process.env.PUBLIC_URL = ''`（空字符串）
   - 生产环境：`process.env.PUBLIC_URL = 'https://jsheng0722.github.io'`
   - 这会导致资源路径不同

2. **构建缓存**
   - 旧的构建文件可能被缓存
   - 需要清除 `build` 目录后重新构建

3. **GitHub Pages 缓存**
   - GitHub Pages 可能有 CDN 缓存
   - 需要等待几分钟或强制刷新

### 验证步骤

1. **清除并重新构建**：
   ```bash
   # 删除旧的构建文件
   rm -rf build
   
   # 重新构建
   npm run build
   
   # 检查 build 目录
   ls -la build/
   # 应该看到 index.html, static/, .nojekyll 等
   ```

2. **检查构建输出**：
   - 打开 `build/index.html`
   - 检查资源路径是否正确
   - 检查是否有绝对路径问题

3. **部署并验证**：
   ```bash
   npm run deploy
   
   # 检查 gh-pages 分支
   git checkout gh-pages
   ls -la
   # 应该看到 build 目录的内容
   ```

4. **在线验证**：
   - 访问 `https://jsheng0722.github.io`
   - 打开浏览器开发者工具（F12）
   - 检查 Console 是否有错误
   - 检查 Network 标签，查看资源加载情况
   - 检查是否有 404 错误

### 常见问题

#### 问题：资源加载失败（404）

**检查**：
- 资源路径是否正确
- `process.env.PUBLIC_URL` 的使用是否正确
- 文件是否存在于 `public` 目录

**解决方案**：
- 使用相对路径：`/images/avatar.png` 而不是 `${process.env.PUBLIC_URL}/images/avatar.png`
- 或者确保 `process.env.PUBLIC_URL` 正确设置

#### 问题：样式丢失

**检查**：
- CSS 文件是否正确加载
- 检查 Network 标签中的 CSS 请求

**解决方案**：
- 确保 Tailwind CSS 正确构建
- 检查 `build/static/css/` 目录

#### 问题：路由不工作

**检查**：
- 直接访问子路由（如 `/notes`）是否工作
- 检查是否有 404 错误

**解决方案**：
- GitHub Pages 不支持客户端路由的服务器端重定向
- 考虑使用 HashRouter（可选）

## 📝 当前配置

- **homepage**: `https://jsheng0722.github.io`
- **部署分支**: `gh-pages`
- **构建目录**: `build`
- **Jekyll**: 已禁用（`.nojekyll` 文件）

## 🚀 部署命令

```bash
# 1. 清除旧的构建
rm -rf build

# 2. 构建项目
npm run build

# 3. 检查构建输出
ls -la build/

# 4. 部署到 GitHub Pages
npm run deploy

# 5. 等待几分钟后访问
# https://jsheng0722.github.io
```

## 🔧 调试技巧

1. **本地测试构建**：
   ```bash
   npm run build
   npx serve -s build
   # 访问 http://localhost:3000
   ```

2. **检查 gh-pages 分支**：
   ```bash
   git checkout gh-pages
   ls -la
   cat index.html
   ```

3. **强制刷新浏览器**：
   - Windows: Ctrl + Shift + R
   - Mac: Cmd + Shift + R
