# 修复配置冲突 - GitHub Pages 显示 README

## 🚨 最可能的原因

即使 Source 设置为 "GitHub Actions"，如果 **`gh-pages` 分支仍然存在**，GitHub Pages 可能会优先使用分支内容而不是 Actions artifact。

## ✅ 立即修复步骤

### 步骤 1: 检查是否存在 gh-pages 分支

在 GitHub 仓库中：

1. 进入 **Code** 标签页
2. 点击分支下拉菜单（显示当前分支的地方）
3. 查看是否有 `gh-pages` 分支

**如果有 `gh-pages` 分支** → 这是问题！

### 步骤 2: 删除 gh-pages 分支

#### 方法 A: 在 GitHub 网页删除（推荐）

1. 进入 **Settings** → **Branches**
2. 找到 `gh-pages` 分支
3. 点击删除按钮

或：

1. 进入 **Code** 标签页
2. 切换到 `gh-pages` 分支
3. 进入 **Settings** → **General** → **Danger Zone**
4. 删除分支

#### 方法 B: 使用 Git 命令删除

```bash
# 删除远程 gh-pages 分支
git push origin --delete gh-pages

# 删除本地 gh-pages 分支（如果存在）
git branch -D gh-pages
```

### 步骤 3: 确认 GitHub Actions 部署成功

1. 进入 **Actions** 标签页
2. 查看最新的 "Deploy to GitHub Pages" 工作流
3. 确认：
   - ✅ Build 步骤成功
   - ✅ Upload artifact 步骤成功
   - ✅ Deploy 步骤成功

### 步骤 4: 手动触发重新部署

1. 在 **Actions** 页面
2. 选择 "Deploy to GitHub Pages" 工作流
3. 点击 **"Run workflow"**
4. 选择 `main` 分支
5. 点击 **"Run workflow"**
6. 等待部署完成

### 步骤 5: 等待并验证

1. **等待 5-10 分钟**（让 GitHub Pages 更新）
2. **清除浏览器缓存**（Ctrl+Shift+Delete）
3. **使用无痕模式访问**：https://jsheng0722.github.io
4. **应该看到**：React 应用（不是 README）

## 🔍 其他可能的冲突

### 冲突 1: package.json 中的 deploy 脚本

虽然不影响 GitHub Actions，但可以清理：

```json
// 当前
"scripts": {
  "deploy": "gh-pages -d build"  // 这个脚本会推送到 gh-pages 分支
}

// 可以改为（可选）
"scripts": {
  "deploy-legacy": "gh-pages -d build"  // 重命名，避免误用
}
```

### 冲突 2: gh-pages 包

虽然不再需要，但可以保留作为备用。如果确定不再使用，可以删除：

```bash
npm uninstall gh-pages
```

## 📋 完整检查清单

修复后确认：

- [ ] **gh-pages 分支已删除**（最重要！）
- [ ] GitHub Pages Source 设置为 "GitHub Actions"
- [ ] GitHub Actions 工作流成功运行（绿色 ✓）
- [ ] 等待了 5-10 分钟
- [ ] 清除了浏览器缓存
- [ ] 访问网站看到 React 应用

## 🆘 如果问题仍然存在

### 检查 1: 查看 GitHub Pages 部署历史

1. 进入 **Settings** → **Pages**
2. 查看部署历史
3. 确认最新的部署来自：
   - ✅ GitHub Actions（正确）
   - ❌ gh-pages 分支（错误，需要删除分支）

### 检查 2: 查看 Actions 日志

1. 进入 **Actions** 标签页
2. 点击最新的工作流运行
3. 查看 "Deploy to GitHub Pages" 步骤的日志
4. 确认部署成功

### 检查 3: 验证 artifact 内容

在 Actions 的 "Upload artifact" 步骤中：

1. 点击查看 artifact
2. 确认包含：
   - `index.html`
   - `.nojekyll`
   - `static/` 目录
   - 所有资源文件

---

**最后更新**: 2025-01-25  
**状态**: ✅ 配置冲突修复指南完成
