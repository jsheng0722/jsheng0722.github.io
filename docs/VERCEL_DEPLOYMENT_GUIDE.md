# Vercel 部署验证指南

本项目同时配置了 **Vercel** 和 **GitHub Pages**，用于不同的目的：

## 🎯 双平台策略

### Vercel - 快速验证环境
- ✅ **用途**：快速验证构建是否成功
- ✅ **优势**：构建速度快，立即看到结果
- ✅ **域名**：`your-project.vercel.app`
- ✅ **触发**：每次推送到 GitHub 自动部署

### GitHub Pages - 正式部署环境
- ✅ **用途**：正式的生产环境
- ✅ **优势**：使用自定义域名 `jsheng0722.github.io`
- ✅ **触发**：通过 GitHub Actions 自动部署

## 📋 Vercel 配置

### 配置文件：`vercel.json`

```json
{
  "buildCommand": "npm run generate-architecture && npm run build",
  "outputDirectory": "build",
  "devCommand": "npm start",
  "installCommand": "npm ci",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### 配置说明

1. **buildCommand**: 构建前先生成架构数据，然后构建 React 应用
2. **outputDirectory**: 输出目录为 `build`
3. **rewrites**: SPA 路由重写规则，确保所有路由都指向 `index.html`

## 🔍 如何使用 Vercel 验证部署

### 1. 查看构建状态

1. 访问：https://vercel.com/dashboard
2. 找到你的项目
3. 查看最新的部署状态：
   - ✅ 绿色 = 构建成功
   - ❌ 红色 = 构建失败（查看日志）

### 2. 验证构建结果

每次推送到 GitHub 后：

1. **Vercel 自动构建**（通常 1-2 分钟）
   - 检查构建日志
   - 确认没有错误
   - 查看预览 URL

2. **如果 Vercel 构建成功**
   - ✅ 说明代码可以正常构建
   - ✅ 可以安全地等待 GitHub Pages 部署

3. **如果 Vercel 构建失败**
   - ❌ 说明代码有问题
   - ❌ 需要修复后再推送到 GitHub
   - ❌ GitHub Pages 部署也会失败

### 3. 工作流程

```
推送代码到 GitHub
    ↓
Vercel 自动构建（1-2分钟）← 快速验证
    ↓
如果成功 → GitHub Actions 部署到 Pages（5-10分钟）← 正式部署
```

## ⚠️ 注意事项

### Babel 警告

你可能会看到这个警告：
```
npm warn deprecated @babel/plugin-proposal-private-property-in-object@7.21.11
```

**这是正常的**，不影响功能。如果需要修复，可以：

```bash
npm install --save-dev @babel/plugin-transform-private-property-in-object
```

然后在 `package.json` 中替换旧的插件。

### 构建命令

Vercel 会自动使用 `vercel.json` 中的 `buildCommand`，确保：
1. 先运行 `generate-architecture` 生成架构数据
2. 然后运行 `build` 构建应用

### SPA 路由

`vercel.json` 中的 `rewrites` 配置确保 React Router 的路由在 Vercel 上正常工作。

## 🛠️ 故障排除

### Vercel 构建失败

1. **检查构建日志**
   - 在 Vercel Dashboard 中查看详细错误
   - 通常错误信息很明确

2. **常见问题**：
   - 依赖安装失败 → 检查 `package.json`
   - 构建脚本错误 → 检查 `buildCommand`
   - 环境变量缺失 → 在 Vercel 设置中配置

### Vercel 和 GitHub Pages 结果不一致

如果 Vercel 成功但 GitHub Pages 失败：

1. **检查 GitHub Actions 日志**
2. **可能的原因**：
   - GitHub Actions 环境不同
   - 权限问题
   - 部署配置问题

## ✨ 最佳实践

1. **先看 Vercel**：推送代码后，先检查 Vercel 构建结果
2. **快速反馈**：Vercel 通常 1-2 分钟就能看到结果
3. **正式部署**：Vercel 成功后，等待 GitHub Pages 部署完成
4. **双重验证**：两个平台都成功，说明部署完全正常

---

**最后更新**: 2025-01-25  
**状态**: ✅ Vercel 配置完成，用于快速验证部署
