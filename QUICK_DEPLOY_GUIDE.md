# 快速部署指南

## 🚀 一键部署步骤

### 1. 确保代码已提交
```bash
git status
git add .
git commit -m "准备部署"
git push origin main
```

### 2. 清除并重新构建
```bash
# 清除旧的构建
rm -rf build

# 重新构建
npm run build
```

### 3. 部署到 GitHub Pages
```bash
npm run deploy
```

### 4. 验证部署
```bash
# 运行部署检查
npm run check-deployment
```

### 5. 检查 GitHub Pages 设置
访问：https://github.com/jsheng0722/jsheng0722.github.io/settings/pages

确认：
- Source: `Deploy from a branch`
- Branch: `gh-pages`
- Folder: `/ (root)`

### 6. 等待并访问
- 等待 5-10 分钟
- 访问：https://jsheng0722.github.io
- 使用 `Ctrl + Shift + R` 强制刷新

## ⚠️ 常见问题

### 问题：部署后网站没有更新
1. 等待 5-10 分钟（GitHub Pages 需要时间）
2. 清除浏览器缓存
3. 检查 GitHub Pages 设置是否正确
4. 检查 `gh-pages` 分支是否已更新

### 问题：资源加载失败
- 确保 `package.json` 中的 `homepage` 是 `https://jsheng0722.github.io`
- 确保 `build` 目录中有 `.nojekyll` 文件

### 问题：页面显示空白
- 检查浏览器控制台（F12）是否有错误
- 确保路由配置正确
- 检查 `.nojekyll` 文件是否存在

## 📝 检查清单

部署前：
- [ ] 代码已提交
- [ ] `build` 目录已清除
- [ ] `npm run build` 成功

部署后：
- [ ] `npm run deploy` 成功
- [ ] GitHub Pages 设置正确
- [ ] 等待 5-10 分钟
- [ ] 清除浏览器缓存
- [ ] 访问网站验证
